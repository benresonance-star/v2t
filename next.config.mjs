/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    localPatterns: [
      {
        pathname: '/logo.png',
        search: '',
      },
    ],
  },
};

export default nextConfig;
